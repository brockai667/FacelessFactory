# Daemon (nahrávanie + lokálny prepis + vloženie textu)
sounddevice>=0.5
numpy>=1.26
faster-whisper>=1.1
pynput>=1.8
pyperclip>=1.9
# Čistenie diktátu cez Claude (voliteľné – bez kľúča sa použijú len pravidlá)
anthropic>=1.0
# Ikona v lište + oznámenia (režim --tray / autoštart)
pystray>=0.19
Pillow>=10
# Hlas naspäť (Diktat hlas): neurónové hlasy Microsoft (edge-tts) + lokálny MCP server pre Claude desktop
edge-tts>=7
mcp>=1.8
